# Test/dev-only dependencies -- not needed in production. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest==8.3.4
pytest-flask==1.3.0
pytest-cov==6.0.0
